extends layout/main-layout.pug
block content 
    .searchBarContainer 
        i.fas.fa-search 
        input#searchBox(type="text",name="search",data-search=selectedTab,placeholder="Search something")
    .tabsContainer 
        //- +createTab("Posts",`/search/posts`,selectedTab != "users") 
        +createTab("Users",`/search/users`,selectedTab == "users")
    .resultsContainer 
block scripts 
    script(src="/js/search.js") 